services:
  api:
    image: calendar-api
    build: ./api
    container_name: calendar-api
    ports:
      - "23000:3000"

  web:
    image: calendar-web
    build: ./web
    container_name: calendar-web
    ports:
      - "28080:80"
    depends_on:
      - api